version: "3"
services:
  web:
    build: ./nginx
    ports:
      - "80:80"
      - "443:443"
    links:
      - app
  app:
    build: ./confluence
    volumes:
      - /var/confluence:/var/atlassian/application-data/confluence
